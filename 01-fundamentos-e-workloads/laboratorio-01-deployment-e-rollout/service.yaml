apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: laboratorio-rollout
  labels:
    app.kubernetes.io/name: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
